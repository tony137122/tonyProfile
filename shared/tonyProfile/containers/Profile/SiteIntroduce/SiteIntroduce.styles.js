import styled from 'styled-components';
import { palette } from 'styled-theme';

const SiteIntroduce = styled.div`
  display: flex;
  flex-wrap: wrap; 
  justify-content: space-between;
`;

export default SiteIntroduce;